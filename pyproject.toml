[project]
name = "payroll-calculator"
version = "0.1.0"
description = "A simplified payroll calculator for Hungarian employee salaries"
license = {text = "MIT"}
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "fastapi-mcp>=0.3.3",
]
